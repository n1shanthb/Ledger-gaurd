/** @deprecated Phase A — Sentinel merged into clerk / solver. */
export async function runSentinel(): Promise<string> {
  throw new Error("runSentinel removed — use runClerk / runSolver (Phase A)");
}
